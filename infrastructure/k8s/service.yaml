apiVersion: v1
kind: Service
metadata:
  name: loyalty-bff
  namespace: loyalty
  labels:
    app: loyalty-bff
spec:
  type: ClusterIP
  selector:
    app: loyalty-bff
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
